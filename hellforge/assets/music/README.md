# Hellforge scene BGM

| File | Phase | Notes |
|------|--------|--------|
| `bgm-camp.mp3` | `camp` (+ title / char UI / wild) | Desecrated Cathedral |
| `bgm-den.mp3` | `den` | Priestess of the Temple |

Playback contract lives in `src/bgm.ts` (HTMLAudio + crossfade). SFX stays in `src/sfx.ts` (synth).
