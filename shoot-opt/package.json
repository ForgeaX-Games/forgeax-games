{
  "name": "@forgeax/game-shoot-opt",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "description": "Minimal GameEntry template -- spinning cubes. Loaded by apps/preview via loadGame dynamic import (feat-20260526-preview-runtime-host M3).",
  "scripts": {},
  "forgeax": {
    "assets": {
      "roots": ["assets"]
    },
    "metrics": {
      "bundle-size": {
        "enabled": false,
        "reason": "template bundle is downstream of the host app (apps/preview)"
      },
      "fps": {
        "enabled": false,
        "reason": "template has no independent smoke gate"
      },
      "bench": {
        "enabled": false,
        "reason": "template scaffold; bench focus stays on engine packages"
      },
      "gate": {
        "enabled": false,
        "reason": "no CI gate on template scaffold; apps/preview hosts acceptance"
      },
      "spike-report": {
        "enabled": false,
        "reason": "not a spike app"
      }
    }
  },
  "dependencies": {
    "@forgeax/engine-app": "workspace:*",
    "@forgeax/engine-runtime": "workspace:*",
    "@forgeax/engine-ecs": "workspace:*",
    "@forgeax/engine-pack": "workspace:*"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.70"
  }
}